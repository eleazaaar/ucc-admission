document.addEventListener('DOMContentLoaded', function() {
  window.stepper = new Stepper(document.querySelector('.bs-stepper'))
})

// setInterval(() => {
//     average_class = $("#class").val();
//     if (average_class !== "" && !$('#grade').is(':focus'))
//       populateSchedule();
// }, 100);